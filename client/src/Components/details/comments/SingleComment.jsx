import { Box, Typography } from "@mui/material";

const SingleComment = ({ allcomment }) => {
  return (
    <Box>
      {/* {allcomment?.map((comment) => (
        <Typography>{comment.data}</Typography>
      ))} */}
    </Box>
  );
};

export default SingleComment;
